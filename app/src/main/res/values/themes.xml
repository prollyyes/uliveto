<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme: no action bar (Compose handles the UI entirely) -->
    <style name="Theme.Uliveto" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
