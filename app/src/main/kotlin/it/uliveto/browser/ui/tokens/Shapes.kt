package it.uliveto.browser.ui.tokens

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

/** Full pill shape — 50% corner radius. */
val PillShape = RoundedCornerShape(50)

/**
 * Hourglass shape stub — real clipped path arrives in M4.
 * Currently approximated as a rounded rectangle.
 */
val HourglassShape = RoundedCornerShape(22.dp)
